name: Patch check

on:
  push:
  pull_request:

jobs:
  patch-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check OpenWrt patch applies
        run: ./scripts/patch-check.sh
